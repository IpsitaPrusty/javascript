const name = "ipsita"
const repoCount = 50

console.log(`Hello my name is ${name} and my repo count is {repoCount}`);

const n = new String('ipsi')
console.log(n[0]);
console.log(n.__proto__);// we can see this using google console

console.log(n.length);
console.log(n.toUpperCase());
console.log(n.charAt('2'));
console.log(n.indexOf('p'));
//substring and slice is also used

 //replace and include- it checks whther that word is present or not  